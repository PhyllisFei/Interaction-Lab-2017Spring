void setup() {
  size(500,600);
  background(0);
}

void draw() {
  //background(0);
  //mousePressed: environment value
  if (mousePressed == true); {
  background(random(255), random(255), random(255) );
  }
  
}

void mousePressed() {
  //background(random(255), random(255), random(255) );

}