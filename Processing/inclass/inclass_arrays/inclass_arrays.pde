int[] number = {0, 7, 8};


void setup() {
  size(500, 600);
  //println(number[2]);
  //for (from, to, inc)
  //i++ : i = i+1
  //o- number.length-1
  for (int i = 0; i < number.length; i++) { //a block of code}
    println( number[i] );
  }
}

void draw() {
  background(0);
}