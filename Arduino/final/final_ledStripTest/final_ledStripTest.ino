void setup() {
  // put your setup code here, to run once:

}

void loop() {
  strip.setPixelColor(0, 0, 0, 255); // (led number, r,g,b);
  strip.setPixelColor(1, 0, 0, 255);
  strip.setPixelColor(2, 0, 0, 255);
  strip.setPixelColor(3, 0, 0, 255);
  strip.setPixelColor(4, 0, 0, 255);
  strip.show();
}
